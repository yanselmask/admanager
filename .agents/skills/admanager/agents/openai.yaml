interface:
  display_name: "Admanager"
  short_description: "Repo-specific patterns and workflows for admanager"
  default_prompt: "Use the admanager repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true